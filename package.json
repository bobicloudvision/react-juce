{
  "name": "blueprint",
  "version": "0.1.0",
  "description": "Write cross-platform native apps with React and JUCE",
  "directories": {
    "example": "examples"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nick-thompson/react-juce.git"
  },
  "devDependencies": {
    "husky": "^4.3.8",
    "lint-staged": "^10.5.3",
    "prettier": "2.2.1"
  },
  "scripts": {
    "beautify": "prettier --write .",
    "lint": "prettier --check .",
    "configure": "cmake -B build -S . -DCMAKE_BUILD_TYPE=Release",
    "build:jsui": "npm --prefix examples/GainPlugin/jsui install && npm --prefix examples/GainPlugin/jsui run build",
    "build:native": "cmake --build build --parallel",
    "build:gainplugin": "npm run build:jsui && npm run build:native",
    "build": "npm run configure && npm run build:gainplugin"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,css,md,yml,yaml}": "prettier --write"
  }
}
