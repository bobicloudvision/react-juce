Bundled via CMake juce_add_binary_data (see ../CMakeLists.txt) and registered in PluginProcessor.cpp.

Inter-Regular.ttf — Inter (https://rsms.me/inter/) — SIL Open Font License 1.1

Outfit-Regular.ttf — static TTF from Outfitio/Outfit-Fonts (fonts/ttf/Outfit-Regular.ttf),
SIL Open Font License (same family as Google Fonts “Outfit”). Variable fonts were replaced with
this static file so the default instance matches “Outfit Regular” and contrasts clearly with Inter.

Replace or add files as needed; update CMake SOURCES and BinaryData:: symbols in PluginProcessor.cpp.
